# A tiny fake app: two worker modules + one entry point (../ex10_two_files.py).
# The whole point: these modules NEVER configure logging. They only ask for a
# logger and use it. The entry point decides where everything goes.
